description "{{ project_name }}"

start on (filesystem)
stop on runlevel [016]

respawn
setuid www-data
setgid www-data
chdir {{ project_path }}

exec {{ venv_path }}/bin/gunicorn --bind 127.0.0.1:8000 wsgi